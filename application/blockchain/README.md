# Finex Smart Contracts

BSC Testnet / Mainnet vault for the existing Laravel Finex platform.

```bash
npm install
npx hardhat test
DEPLOY_MOCK_USDT=1 npx hardhat run scripts/deploy.js --network bscTestnet
```

See `../docs/BLOCKCHAIN_INTEGRATION.md` for Laravel wiring and `.env` keys.
